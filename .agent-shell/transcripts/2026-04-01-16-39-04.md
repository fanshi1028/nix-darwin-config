# Agent Shell Transcript

**Agent:** OpenCode
**Started:** 2026-04-01 16:39:43
**Working Directory:** /private/etc/nix-darwin/

---

## User (2026-04-01 16:39:43)

add the lastest emacs via https://github.com/nix-community/emacs-overlay

## Agent's Thoughts (2026-04-01 16:40:13)


User wants to add Emacs via the emacs-overlay in their nix-darwin configuration, so I should first explore the repository structure to understand how to properly integrate it.


### Tool Call [completed]: glob

**Tool:** search
**Timestamp:** 2026-04-01 16:40:15
**Parameters:**
pattern: **/*.nix

```
/private/etc/nix-darwin/flake.nix
/private/etc/nix-darwin/obs-studio.nix
```
## Agent's Thoughts (2026-04-01 16:40:16)


I need to read the flake.nix file to understand the current configuration structure before adding the emacs-overlay.


### Tool Call [completed]: flake.nix

**Tool:** read
**Timestamp:** 2026-04-01 16:40:18
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix

```
<path>/private/etc/nix-darwin/flake.nix</path>
<type>file</type>
<content>1: {
2:   description = "Fanshi1028's nix-darwin system flake";
3:   inputs = {
4:     nixpkgs.url = "github:NixOS/nixpkgs/nixpkgs-25.11-darwin";
5:     nixpkgs-unstable.url = "github:NixOS/nixpkgs/nixos-unstable";
6:     nix-darwin.url = "github:nix-darwin/nix-darwin/nix-darwin-25.11";
7:     nix-darwin.inputs.nixpkgs.follows = "nixpkgs";
8:     kmonad.url = "git+https://github.com/kmonad/kmonad?submodules=1&dir=nix";
9:     kmonad.inputs.nixpkgs.follows = "nixpkgs";
10:   };
11: 
12:   outputs =
13:     inputs@{
14:       self,
15:       nix-darwin,
16:       nixpkgs,
17:       nixpkgs-unstable,
18:       kmonad,
19:     }:
20:     let
21:       system = "aarch64-darwin";
22:       kmonad-exe = kmonad.packages."${system}".default;
23:       configuration =
24:         { pkgs, ... }:
25:         let
26:           kmonadConfigFile = pkgs.writeText "kmonad-config.kbd" (builtins.readFile ./home-row-mod.kbd);
27:         in
28:         {
29:           networking = {
30:             knownNetworkServices = [ "Wi-Fi" ];
31:             dns = [
32:               # adguard
33:               "94.140.14.14"
34:               "94.140.15.15"
35:               "2a10:50c0::ad1:ff"
36:               "2a10:50c0::ad2:ff"
37:               #quad9
38:               "9.9.9.9"
39:               "149.112.112.112"
40:               "2620:fe::fe"
41:               "2620:fe::9"
42:               #cloudflare
43:               "1.1.1.1"
44:               "1.0.0.1"
45:               "2606:4700:4700::1111"
46:               "2606:4700:4700::1001"
47:               # google
48:               "8.8.8.8"
49:               "8.8.4.4"
50:               "2001:4860:4860::8888"
51:               "2001:4860:4860::8844"
52:             ];
53:           };
54:           programs = {
55:             fish = {
56:               enable = true;
57:               promptInit = ''
58:                 ${builtins.readFile ./fish/fish_prompt.fish}
59:                 ${builtins.readFile ./fish/fish_right_prompt.fish}
60:               '';
61:               interactiveShellInit = "set -U fish_greeting";
62:             };
63:             direnv.enable = true;
64:           };
65: 
66:           fonts.packages = with pkgs; [
67:             nerd-fonts.symbols-only
68:             sarasa-gothic
69:           ];
70: 
71:           nix.channel.enable = false;
72: 
73:           nix.nixPath = [
74:             {
75:               nixpkgs = "flake:nixpkgs";
76:               nixpkgs-unstable = "flake:nixpkgs-unstable";
77:             }
78:           ];
79: 
80:           nix.extraOptions = ''
81:             keep-outputs = true
82:             keep-derivations = true
83:           '';
84: 
85:           nix.settings = {
86:             trusted-public-keys = [
87:               "fanshi1028-personal.cachix.org-1:XoynOisskxlhrHM+m5ytvodedJdAo8gKpam/L6/AmBI="
88:               "nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=  "
89:               "haskell-miso-cachix.cachix.org-1:m8hN1cvFMJtYib4tj+06xkKt5ABMSGfe8W7s40x1kQ0="
90:             ];
91:             trusted-substituters = [
92:               "https://fanshi1028-personal.cachix.org"
93:               "https://nix-community.cachix.org"
94:               "https://haskell-miso-cachix.cachix.org"
95:             ];
96:           };
97: 
98:           nix.registry = {
99:             nixpkgs = {
100:               flake = nixpkgs;
101:             };
102:             nixpkgs-unstable = {
103:               flake = nixpkgs-unstable;
104:             };
105:           };
106: 
107:           environment.variables = {
108:             EDITOR = "vim";
109:           };
110:           environment.systemPackages =
111:             (
112:               with pkgs;
113:               [
114:                 mpv
115:                 vim
116:                 cachix
117:                 git
118:                 emacs-pgtk
119:                 localsend
120:                 nix-output-monitor
121:                 ripgrep
122:                 zstd
123:                 nixfmt-rfc-style
124:                 ffmpeg
125:                 emacs-lsp-booster
126:                 python313Packages.huggingface-hub
127:                 # TEMP for doom emacs env: https://github.com/doomemacs/doomemacs/issues/6612#issuecomment-4150181313
128:                 fd
129:                 tree
130:               ]
131:               # default env to for using cabal inti to quick start haskell project
132:               ++ [
133:                 haskell.compiler.ghc9122
134:                 cabal-install
135:               ]
136:               ++ lib.optionals (system == "aarch64-darwin") [
137:                 (callPackage ./obs-studio.nix { })
138:               ]
139:             )
140:             ++ (with import nixpkgs-unstable { inherit system; }; [
141:               yt-dlp
142: 
143:               (llama-cpp.overrideAttrs ({
144:                 version = "8505";
145:                 src = fetchFromGitHub {
146:                   owner = "ggml-org";
147:                   repo = "llama.cpp";
148:                   tag = "b8505";
149:                   hash = "sha256-Bg7fUTHsXcwEdemi0/T4GXB09SOx4UHZ7clN9zQ1zDA=";
150:                   leaveDotGit = true;
151:                   postFetch = ''
152:                     git -C "$out" rev-parse --short HEAD > $out/COMMIT
153:                     find "$out" -name .git -print0 | xargs -0 rm -rf
154:                   '';
155:                 };
156:                 npmDepsHash = "sha256-DxgUDVr+kwtW55C4b89Pl+j3u2ILmACcQOvOBjKWAKQ=";
157:               }))
158:               python314Packages.mlx-lm
159:               stable-diffusion-cpp
160:             ]);
161: 
162:           # Necessary for using flakes on this system.
163:           nix.settings.experimental-features = "nix-command flakes";
164: 
165:           # Set Git commit hash for darwin-version.
166:           system.configurationRevision = self.rev or self.dirtyRev or null;
167: 
168:           # Used for backwards compatibility, please read the changelog before changing.
169:           # $ darwin-rebuild changelog
170:           system.stateVersion = 6;
171: 
172:           # The platform the configuration will be used on.
173:           nixpkgs.hostPlatform = system;
174: 
175:           launchd.daemons.Karabiner-VirtualHIDDevice-Daemon = {
176:             serviceConfig = {
177:               Program = "/Library/Application Support/org.pqrs/Karabiner-DriverKit-VirtualHIDDevice/Applications/Karabiner-VirtualHIDDevice-Daemon.app/Contents/MacOS/Karabiner-VirtualHIDDevice-Daemon";
178:               KeepAlive = true;
179:               RunAtLoad = true;
180:               StandardOutPath = "/tmp/Karabiner-DriverKit-VirtualHIDDevice.log";
181:               StandardErrorPath = "/tmp/Karabiner-DriverKit-VirtualHIDDevice.err";
182:             };
183:           };
184:           # FIXME: not work due to https://github.com/kmonad/kmonad/issues/675
185:           # launchd.daemons.kmonad = with pkgs; {
186:           #   path = [ kmonad-exe ];
187:           #   serviceConfig = {
188:           #     Program = "${kmonad-exe}/bin/kmonad";
189:           #     ProgramArguments = [
190:           #       "${kmonad-exe}/bin/kmonad"
191:           #       "${kmonadConfigFile}"
192:           #     ];
193:           #     KeepAlive = true;
194:           #     RunAtLoad = true;
195:           #     StandardOutPath = "/tmp/kmonad.log";
196:           #     StandardErrorPath = "/tmp/kmonad.err";
197:           #     # Optional: delay startup if HID services aren't ready
198:           #     # throttleInterval = 5;
199:           #   };
200:           # };
201: 
202:           security.pam.services.sudo_local.touchIdAuth = true;
203:         };
204:     in
205:     {
206:       # Build darwin flake using:
207:       # $ darwin-rebuild build --flake .#Francis
208:       darwinConfigurations.Franciss-MacBook-Pro = nix-darwin.lib.darwinSystem {
209:         modules = [ configuration ];
210:       };
211:     };
212: }

(End of file - total 212 lines)
</content>
```
## Agent's Thoughts (2026-04-01 16:40:26)


Now I understand the flake structure and need to add the emacs-overlay input and update the system packages to use emacs from the overlay instead of the current emacs-pgtk.

## Agent (2026-04-01 16:40:27)

I'll add the emacs-overlay to your flake.nix and update the emacs package to use the latest version from the overlay.



### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:40:47
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString:  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixpkgs-25.11-darwin";
    nixpkgs-unstable.url = "github:NixOS/nixpkgs/nixos-unstable";
    nix-darwin.url = "github:nix-darwin/nix-darwin/nix-darwin-25.11";
    nix-darwin.inputs.nixpkgs.follows = "nixpkgs";
    kmonad.url = "git+https://github.com/kmonad/kmonad?submodules=1&dir=nix";
    kmonad.inputs.nixpkgs.follows = "nixpkgs";
  };
newString:  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixpkgs-25.11-darwin";
    nixpkgs-unstable.url = "github:NixOS/nixpkgs/nixos-unstable";
    nix-darwin.url = "github:nix-darwin/nix-darwin/nix-darwin-25.11";
    nix-darwin.inputs.nixpkgs.follows = "nixpkgs";
    kmonad.url = "git+https://github.com/kmonad/kmonad?submodules=1&dir=nix";
    kmonad.inputs.nixpkgs.follows = "nixpkgs";
    emacs-overlay.url = "github:nix-community/emacs-overlay";
    emacs-overlay.inputs.nixpkgs.follows = "nixpkgs";
  };

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -5,4 +5,6 @@
     nix-darwin.inputs.nixpkgs.follows = "nixpkgs";
     kmonad.url = "git+https://github.com/kmonad/kmonad?submodules=1&dir=nix";
     kmonad.inputs.nixpkgs.follows = "nixpkgs";
+    emacs-overlay.url = "github:nix-community/emacs-overlay";
+    emacs-overlay.inputs.nixpkgs.follows = "nixpkgs";
   };
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:40:49)


Now I need to update the outputs function signature to include emacs-overlay and then update the package references to use emacs from the overlay.


### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:40:57
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString:  outputs =
    inputs@{
      self,
      nix-darwin,
      nixpkgs,
      nixpkgs-unstable,
      kmonad,
    }:
newString:  outputs =
    inputs@{
      self,
      nix-darwin,
      nixpkgs,
      nixpkgs-unstable,
      kmonad,
      emacs-overlay,
    }:

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -5,4 +5,5 @@
       nixpkgs,
       nixpkgs-unstable,
       kmonad,
+      emacs-overlay,
     }:
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:40:59)


Now I need to update the configuration function to accept emacs-overlay as a parameter so it can be used in the package override.


### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:41:19
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString: outputs =
    inputs@{
      self,
      nix-darwin,
      nixpkgs,
      nixpkgs-unstable,
      kmonad,
      emacs-overlay,
    }:
    let
      system = "aarch64-darwin";
      kmonad-exe = kmonad.packages."${system}".default;
      configuration =
        { pkgs, ... }:
newString: outputs =
    inputs@{
      self,
      nix-darwin,
      nixpkgs,
      nixpkgs-unstable,
      kmonad,
      emacs-overlay,
    }:
    let
      system = "aarch64-darwin";
      kmonad-exe = kmonad.packages."${system}".default;
      emacs-pkgs = import emacs-overlay.nix {
        inherit system;
        nixpkgs = import nixpkgs { inherit system; overlays = [ emacs-overlay.overlay ]; };
      };
      configuration =
        { pkgs, ... }:

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -10,5 +10,9 @@
     let
       system = "aarch64-darwin";
       kmonad-exe = kmonad.packages."${system}".default;
+      emacs-pkgs = import emacs-overlay.nix {
+        inherit system;
+        nixpkgs = import nixpkgs { inherit system; overlays = [ emacs-overlay.overlay ]; };
+      };
       configuration =
         { pkgs, ... }:
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:41:21)


Now I need to replace the emacs-pgtk package in systemPackages with the latest emacs29-pgtk from the overlay.


### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:41:29
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString: [
                 mpv
                 vim
                 cachix
                 git
                 emacs-pgtk
                 localsend
newString: [
                 mpv
                 vim
                 cachix
                 git
                 emacs-pkgs.emacs29-pgtk
                 localsend

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -3,5 +3,5 @@
                  vim
                  cachix
                  git
-                 emacs-pgtk
+                 emacs-pkgs.emacs29-pgtk
                  localsend
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:41:30)


Now I need to update the emacs-lsp-booster reference to use the version from emacs-overlay's nixpkgs for consistency.


### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:41:36
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString: emacs-lsp-booster
newString: emacs-pkgs.emacs-lsp-booster

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -1 +1 @@
-emacs-lsp-booster
\ No newline at end of file
+emacs-pkgs.emacs-lsp-booster
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:41:37)


All edits to add the emacs-overlay and update the emacs packages have been completed successfully.

## Agent (2026-04-01 16:41:38)

Done. I've added the emacs-overlay and updated your configuration to use the latest Emacs from it. Run `darwin-rebuild switch` to apply the changes.

## User (2026-04-01 16:43:25)

take a look at the emacs-overlay repo src code before redoing it

## Agent's Thoughts (2026-04-01 16:43:39)


The user wants me to look at the emacs-overlay repository to understand how it works before I redo the configuration. Let me fetch information from the GitHub repo to understand the proper way to use it.


### Tool Call [completed]: https://github.com/nix-community/emacs-overlay (text/html; charset=utf-8)

**Tool:** fetch
**Timestamp:** 2026-04-01 16:43:43
**Parameters:**
url: https://github.com/nix-community/emacs-overlay
format: markdown

```
GitHub - nix-community/emacs-overlay: Bleeding edge emacs overlay \[maintainer=@adisbladis\] · GitHub                                             

[Skip to content](#start-of-content)   

## Navigation Menu

Toggle navigation

[](/)

[Sign in](/login?return_to=https%3A%2F%2Fgithub.com%2Fnix-community%2Femacs-overlay)

Appearance settings

  

-   Platform
    
    -   AI CODE CREATION
        
        -   [
            
            GitHub CopilotWrite better code with AI
            
            ](https://github.com/features/copilot)
        -   [
            
            GitHub SparkBuild and deploy intelligent apps
            
            ](https://github.com/features/spark)
        -   [
            
            GitHub ModelsManage and compare prompts
            
            ](https://github.com/features/models)
        -   [
            
            MCP RegistryNewIntegrate external tools
            
            ](https://github.com/mcp)
        
    -   DEVELOPER WORKFLOWS
        
        -   [
            
            ActionsAutomate any workflow
            
            ](https://github.com/features/actions)
        -   [
            
            CodespacesInstant dev environments
            
            ](https://github.com/features/codespaces)
        -   [
            
            IssuesPlan and track work
            
            ](https://github.com/features/issues)
        -   [
            
            Code ReviewManage code changes
            
            ](https://github.com/features/code-review)
        
    -   APPLICATION SECURITY
        
        -   [
            
            GitHub Advanced SecurityFind and fix vulnerabilities
            
            ](https://github.com/security/advanced-security)
        -   [
            
            Code securitySecure your code as you build
            
            ](https://github.com/security/advanced-security/code-security)
        -   [
            
            Secret protectionStop leaks before they start
            
            ](https://github.com/security/advanced-security/secret-protection)
        
    -   EXPLORE
        
        -   [Why GitHub](https://github.com/why-github)
        -   [Documentation](https://docs.github.com)
        -   [Blog](https://github.blog)
        -   [Changelog](https://github.blog/changelog)
        -   [Marketplace](https://github.com/marketplace)
        
    
    [View all features](https://github.com/features)
    
-   Solutions
    
    -   BY COMPANY SIZE
        
        -   [Enterprises](https://github.com/enterprise)
        -   [Small and medium teams](https://github.com/team)
        -   [Startups](https://github.com/enterprise/startups)
        -   [Nonprofits](https://github.com/solutions/industry/nonprofits)
        
    -   BY USE CASE
        
        -   [App Modernization](https://github.com/solutions/use-case/app-modernization)
        -   [DevSecOps](https://github.com/solutions/use-case/devsecops)
        -   [DevOps](https://github.com/solutions/use-case/devops)
        -   [CI/CD](https://github.com/solutions/use-case/ci-cd)
        -   [View all use cases](https://github.com/solutions/use-case)
        
    -   BY INDUSTRY
        
        -   [Healthcare](https://github.com/solutions/industry/healthcare)
        -   [Financial services](https://github.com/solutions/industry/financial-services)
        -   [Manufacturing](https://github.com/solutions/industry/manufacturing)
        -   [Government](https://github.com/solutions/industry/government)
        -   [View all industries](https://github.com/solutions/industry)
        
    
    [View all solutions](https://github.com/solutions)
    
-   Resources
    
    -   EXPLORE BY TOPIC
        
        -   [AI](https://github.com/resources/articles?topic=ai)
        -   [Software Development](https://github.com/resources/articles?topic=software-development)
        -   [DevOps](https://github.com/resources/articles?topic=devops)
        -   [Security](https://github.com/resources/articles?topic=security)
        -   [View all topics](https://github.com/resources/articles)
        
    -   EXPLORE BY TYPE
        
        -   [Customer stories](https://github.com/customer-stories)
        -   [Events & webinars](https://github.com/resources/events)
        -   [Ebooks & reports](https://github.com/resources/whitepapers)
        -   [Business insights](https://github.com/solutions/executive-insights)
        -   [GitHub Skills](https://skills.github.com)
        
    -   SUPPORT & SERVICES
        
        -   [Documentation](https://docs.github.com)
        -   [Customer support](https://support.github.com)
        -   [Community forum](https://github.com/orgs/community/discussions)
        -   [Trust center](https://github.com/trust-center)
        -   [Partners](https://github.com/partners)
        
    
    [View all resources](https://github.com/resources)
    
-   Open Source
    
    -   COMMUNITY
        
        -   [
            
            GitHub SponsorsFund open source developers
            
            ](https://github.com/sponsors)
        
    -   PROGRAMS
        
        -   [Security Lab](https://securitylab.github.com)
        -   [Maintainer Community](https://maintainers.github.com)
        -   [Accelerator](https://github.com/accelerator)
        -   [GitHub Stars](https://stars.github.com)
        -   [Archive Program](https://archiveprogram.github.com)
        
    -   REPOSITORIES
        
        -   [Topics](https://github.com/topics)
        -   [Trending](https://github.com/trending)
        -   [Collections](https://github.com/collections)
        
    
-   Enterprise
    
    -   ENTERPRISE SOLUTIONS
        
        -   [
            
            Enterprise platformAI-powered developer platform
            
            ](https://github.com/enterprise)
        
    -   AVAILABLE ADD-ONS
        
        -   [
            
            GitHub Advanced SecurityEnterprise-grade security features
            
            ](https://github.com/security/advanced-security)
        -   [
            
            Copilot for BusinessEnterprise-grade AI features
            
            ](https://github.com/features/copilot/copilot-business)
        -   [
            
            Premium SupportEnterprise-grade 24/7 support
            
            ](https://github.com/premium-support)
        
    
-   [Pricing](https://github.com/pricing)

Search or jump to...

# Search code, repositories, users, issues, pull requests...

Search

Clear

[Search syntax tips](https://docs.github.com/search-github/github-code-search/understanding-github-code-search-syntax)

# Provide feedback

We read every piece of feedback, and take your input very seriously.

 Include my email address so I can be contacted

Cancel Submit feedback

# Saved searches

## Use saved searches to filter your results more quickly

Name  

Query 

To see all available qualifiers, see our [documentation](https://docs.github.com/search-github/github-code-search/understanding-github-code-search-syntax).

Cancel Create saved search

[Sign in](/login?return_to=https%3A%2F%2Fgithub.com%2Fnix-community%2Femacs-overlay)

[Sign up](/signup?ref_cta=Sign+up&ref_loc=header+logged+out&ref_page=%2F%3Cuser-name%3E%2F%3Crepo-name%3E&source=header-repo&source_repo=nix-community%2Femacs-overlay)

Appearance settings

Resetting focus

You signed in with another tab or window. Reload to refresh your session. You signed out in another tab or window. Reload to refresh your session. You switched accounts on another tab or window. Reload to refresh your session. Dismiss alert

[nix-community](/nix-community) / **[emacs-overlay](/nix-community/emacs-overlay)** Public

-   ### Uh oh!
    
    There was an error while loading. Please reload this page.
    
-   [Notifications](/login?return_to=%2Fnix-community%2Femacs-overlay) You must be signed in to change notification settings
-   [Fork 183](/login?return_to=%2Fnix-community%2Femacs-overlay)
-   [Star 613](/login?return_to=%2Fnix-community%2Femacs-overlay)
    

-   [Code](/nix-community/emacs-overlay)
-   [Issues 8](/nix-community/emacs-overlay/issues)
-   [Pull requests 5](/nix-community/emacs-overlay/pulls)
-   [Actions](/nix-community/emacs-overlay/actions)
-   [Projects](/nix-community/emacs-overlay/projects)
-   [Security and quality 0](/nix-community/emacs-overlay/security)
-   [Insights](/nix-community/emacs-overlay/pulse)

Additional navigation options

-   [Code](/nix-community/emacs-overlay)
-   [Issues](/nix-community/emacs-overlay/issues)
-   [Pull requests](/nix-community/emacs-overlay/pulls)
-   [Actions](/nix-community/emacs-overlay/actions)
-   [Projects](/nix-community/emacs-overlay/projects)
-   [Security and quality](/nix-community/emacs-overlay/security)
-   [Insights](/nix-community/emacs-overlay/pulse)

 [](/nix-community/emacs-overlay)

# nix-community/emacs-overlay

 master

[Branches](/nix-community/emacs-overlay/branches)[Tags](/nix-community/emacs-overlay/tags)

[](/nix-community/emacs-overlay/branches)[](/nix-community/emacs-overlay/tags)

Go to file

Code

Open more actions menu

## Folders and files

Name

Name

Last commit message

Last commit date

## Latest commit

## History

[18,603 Commits](/nix-community/emacs-overlay/commits/master/)

[](/nix-community/emacs-overlay/commits/master/)18,603 Commits

[.github](/nix-community/emacs-overlay/tree/master/.github ".github")

[.github](/nix-community/emacs-overlay/tree/master/.github ".github")

[overlays](/nix-community/emacs-overlay/tree/master/overlays "overlays")

[overlays](/nix-community/emacs-overlay/tree/master/overlays "overlays")

[repos](/nix-community/emacs-overlay/tree/master/repos "repos")

[repos](/nix-community/emacs-overlay/tree/master/repos "repos")

[README.org](/nix-community/emacs-overlay/blob/master/README.org "README.org")

[README.org](/nix-community/emacs-overlay/blob/master/README.org "README.org")

[default.nix](/nix-community/emacs-overlay/blob/master/default.nix "default.nix")

[default.nix](/nix-community/emacs-overlay/blob/master/default.nix "default.nix")

[elisp.nix](/nix-community/emacs-overlay/blob/master/elisp.nix "elisp.nix")

[elisp.nix](/nix-community/emacs-overlay/blob/master/elisp.nix "elisp.nix")

[flake.lock](/nix-community/emacs-overlay/blob/master/flake.lock "flake.lock")

[flake.lock](/nix-community/emacs-overlay/blob/master/flake.lock "flake.lock")

[flake.nix](/nix-community/emacs-overlay/blob/master/flake.nix "flake.nix")

[flake.nix](/nix-community/emacs-overlay/blob/master/flake.nix "flake.nix")

[packreq.nix](/nix-community/emacs-overlay/blob/master/packreq.nix "packreq.nix")

[packreq.nix](/nix-community/emacs-overlay/blob/master/packreq.nix "packreq.nix")

[parse.nix](/nix-community/emacs-overlay/blob/master/parse.nix "parse.nix")

[parse.nix](/nix-community/emacs-overlay/blob/master/parse.nix "parse.nix")

[update](/nix-community/emacs-overlay/blob/master/update "update")

[update](/nix-community/emacs-overlay/blob/master/update "update")

View all files

## Repository files navigation

-   [README](#)
-   [Code of conduct](#)
-   [Security](#)

# Emacs overlay for Nixpkgs

[](#emacs-overlay-for-nixpkgs)

## Quickstart

[](#quickstart)

To get up and running quickly, add the following lines to your `/etc/nixos/configuration.nix`:

{config, pkgs, callPackage, ... }:
{
\# ...

  services.emacs.package \= pkgs.emacs-unstable;

  nixpkgs.overlays \= \[
    (import (builtins.fetchTarball {
      url \= "https://github.com/nix-community/emacs-overlay/archive/master.tar.gz";
    }))
  \];

\# ...
}

This configuration will enable this overlay, and define your system-wide emacs package as the `emacs-unstable` attribute it provides.

**NOTE:** Read the “Usage of the overlay” section below for further explanation of this configuration. This has the potential to break things, and will frequently trigger full source rebuilds of emacs.

If you want to enable daemon/server mode, add the following line to the same configuration:

services.emacs.enable \= true;

It is recommended you read Nixpkgs and NixOS documentation on package overlays and overrides to familiarize yourself with the concepts:

-   [https://wiki.nixos.org/wiki/Overlays](https://wiki.nixos.org/wiki/Overlays)
-   [https://nixos.org/nixpkgs/manual/#chap-overlays](https://nixos.org/nixpkgs/manual/#chap-overlays)

## Contents of the overlay

[](#contents-of-the-overlay)

This overlay consists of two overlays: `emacs` and `package`.

You can use both of them as a whole overlay or only one of them.

### `package` overlay

[](#package-overlay)

#### Elpa

[](#elpa)

Daily generations of Elpa.

#### Melpa / Melpa stable

[](#melpa--melpa-stable)

Daily generations of Melpa & Melpa stable attribute sets.

#### EXWM & needed dependencies

[](#exwm--needed-dependencies)

This overlay provides fresh versions of EXWM and dependencies. This is updated daily.

### `emacs` overlay

[](#emacs-overlay)

#### Emacs from Git and latest (including pre-releases)

[](#emacs-from-git-and-latest-including-pre-releases)

This overlay also provides two versions (latest from git) for Emacs. These are updated daily.

These attributes are named `emacs-git` and `emacs-unstable`. `emacs-git` is built from the latest `master` branch and `emacs-unstable` is built from the latest tag.

Emacs from git is not guaranteed stable and may break your setup at any time, if it breaks you get to keep both pieces.

We also provide two attributes named `emacs-git-nox` and `emacs-unstable-nox` if you wish to have Emacs built without X dependencies.

Additionally, the two attributes `emacs-git-pgtk` and `emacs-unstable-pgtk` enable the pure GTK (PGTK) feature, which is incompatible with X and supports Wayland natively.

#### Extra library functionality

[](#extra-library-functionality)

This overlay comes with extra functions to generate an Emacs closure from various types of dependency declaration. (These are abstractions on top of `emacsWithPackages`.)

For example, `emacsWithPackagesFromUsePackage` adds packages which are required in a user’s config via `use-package` or `leaf`.

{ pkgs, ... }:
{
  environment.systemPackages \= \[
    (pkgs.emacsWithPackagesFromUsePackage {
      \# Your Emacs config file. Org mode babel files are also
      \# supported.
      \# NB: Config files cannot contain unicode characters, since
      \#     they're being parsed in nix, which lacks unicode
      \#     support.
      \# config = ./emacs.org;
      config \= ./emacs.el;

      \# Whether to include your config as a default init file.
      \# If being bool, the value of config is used.
      \# Its value can also be a derivation like this if you want to do some
      \# substitution:
      \#   defaultInitFile = pkgs.substituteAll {
      \#     name = "default.el";
      \#     src = ./emacs.el;
      \#     inherit (config.xdg) configHome dataHome;
      \#   };
      defaultInitFile \= true;

      \# Package is optional, defaults to pkgs.emacs
      package \= pkgs.emacs-git;

      \# By default emacsWithPackagesFromUsePackage will only pull in
      \# packages with \`:ensure\`, \`:ensure t\` or \`:ensure <package name>\`.
      \# Setting \`alwaysEnsure\` to \`true\` emulates \`use-package-always-ensure\`
      \# and pulls in all use-package references not explicitly disabled via
      \# \`:ensure nil\` or \`:disabled\`.
      \# Note that this is NOT recommended unless you've actually set
      \# \`use-package-always-ensure\` to \`t\` in your config.
      alwaysEnsure \= true;

      \# For Org mode babel files, by default only code blocks with
      \# \`:tangle yes\` are considered. Setting \`alwaysTangle\` to \`true\`
      \# will include all code blocks missing the \`:tangle\` argument,
      \# defaulting it to \`yes\`.
      \# Note that this is NOT recommended unless you have something like
      \# \`#+PROPERTY: header-args:emacs-lisp :tangle yes\` in your config,
      \# which defaults \`:tangle\` to \`yes\`.
      alwaysTangle \= true;

      \# Optionally provide extra packages not in the configuration file.
      \# This can also include extra executables to be run by Emacs (linters,
      \# language servers, formatters, etc)
      extraEmacsPackages \= epkgs: \[
        epkgs.cask
        pkgs.shellcheck
      \];

      \# Optionally override derivations.
      override \= final: prev: {
        weechat \= prev.melpaPackages.weechat.overrideAttrs(old: {
          patches \= \[ ./weechat-el.patch \];
        });
      };
    })
  \];
}

Similarly, `emacsWithPackagesFromPackageRequires` adds packages which are declared in a `.el` package file’s `Package-Requires` header, which can be handy for CI purposes:

...
let
  emacsForCI \= pkgs.emacsWithPackagesFromPackageRequires {
    packageElisp \= builtins.readFile ./flycheck.el;
    extraEmacsPackages \= epkgs: \[
      epkgs.package-lint
    \];
  };
pkgs.mkShell {
  buildInputs \= \[ emacsForCI \];
}

## Usage of the overlay

[](#usage-of-the-overlay)

### Latest master each rebuild

[](#latest-master-each-rebuild)

One way, and probably the most convenient way to pull in this overlay is by just fetching the tarball of latest master on rebuild.

This has side-effects if packages breaks or things like that you may want to be in control of which revision of the overlay you run.

Adding the overlay this way will extend your Emacs packages set to contain the latest EXWM and dependencies from their respective master and make the package `emacs-git` available. These of course change quite rapidly and will cause compilation time.

{
  nixpkgs.overlays \= \[
    (import (builtins.fetchTarball {
      url \= "https://github.com/nix-community/emacs-overlay/archive/master.tar.gz";
    }))
  \];
}

### Binary cache

[](#binary-cache)

You will want to use the [nix-community binary cache](https://nix-community.org/cache/). Where the overlay’s build artefacts are pushed. See [here](https://app.cachix.org/cache/nix-community) for installation instructions.

### Install directly from the overlay

[](#install-directly-from-the-overlay)

The repository is meant to be used as an overlay as is explained above. Still, for experimental purposes, you might want to install a package directly from the overlay. For example, you can install `emacs-git` from a clone of this repository with the following command:

nix-build --expr 'with (import <nixpkgs> { overlays = \[ (import ./.) \]; }); emacs-git'

# Community

[](#community)

## Matrix chat

[](#matrix-chat)

[Nix Emacs](https://matrix.to/#/#emacs:nixos.org)

## About

Bleeding edge emacs overlay \[maintainer=[@adisbladis](https://github.com/adisbladis)\]

### Resources

[Readme](#readme-ov-file)

### Code of conduct

[Code of conduct](#coc-ov-file)

### Security policy

[Security policy](#security-ov-file)

### Uh oh!

There was an error while loading. Please reload this page.

[Activity](/nix-community/emacs-overlay/activity)

[Custom properties](/nix-community/emacs-overlay/custom-properties)

### Stars

[**613** stars](/nix-community/emacs-overlay/stargazers)

### Watchers

[**10** watching](/nix-community/emacs-overlay/watchers)

### Forks

[**183** forks](/nix-community/emacs-overlay/forks)

[Report repository](/contact/report-content?content_url=https%3A%2F%2Fgithub.com%2Fnix-community%2Femacs-overlay&report=nix-community+%28user%29)

## [Releases](/nix-community/emacs-overlay/releases)

No releases published

## Sponsor this project

 ### Uh oh!

There was an error while loading. Please reload this page.

-    ![open_collective](https://github.githubassets.com/assets/open_collective-0a706523753d.svg)[opencollective.com/**nix-community**](https://opencollective.com/nix-community)

[Learn more about GitHub Sponsors](/sponsors)

## [Packages 0](/orgs/nix-community/packages?repo_name=emacs-overlay)

### Uh oh!

There was an error while loading. Please reload this page.

### Uh oh!

There was an error while loading. Please reload this page.

## [Contributors](/nix-community/emacs-overlay/graphs/contributors)

### Uh oh!

There was an error while loading. Please reload this page.

## Languages

-   [Nix 99.8%](/nix-community/emacs-overlay/search?l=nix)
-   [Python 0.2%](/nix-community/emacs-overlay/search?l=python)

## Footer

[](https://github.com)© 2026 GitHub, Inc.

### Footer navigation

-   [Terms](https://docs.github.com/site-policy/github-terms/github-terms-of-service)
-   [Privacy](https://docs.github.com/site-policy/privacy-policies/github-privacy-statement)
-   [Security](https://github.com/security)
-   [Status](https://www.githubstatus.com/)
-   [Community](https://github.community/)
-   [Docs](https://docs.github.com/)
-   [Contact](https://support.github.com?tags=dotcom-footer)
-   Manage cookies
-   Do not share my personal information

You can’t perform that action at this time.
```
## Agent's Thoughts (2026-04-01 16:44:00)


Now I understand how the emacs-overlay works. It provides an overlay that can be applied to nixpkgs, and it exposes packages like `emacs-git-pgtk`, `emacs-unstable-pgtk`, etc. Let me also check the flake.nix in the emacs-overlay repo to see how it's structured for flake usage.


### Tool Call [completed]: https://raw.githubusercontent.com/nix-community/emacs-overlay/master/flake.nix (text/plain; charset=utf-8)

**Tool:** fetch
**Timestamp:** 2026-04-01 16:44:04
**Parameters:**
url: https://raw.githubusercontent.com/nix-community/emacs-overlay/master/flake.nix
format: markdown

```
{
  description = "Bleeding edge Emacs overlay";

  nixConfig = {
    extra-substituters = [ "https://nix-community.cachix.org" ];
    extra-trusted-public-keys = [ "nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=" ];
  };

  inputs = {
    nixpkgs.url = "github:NixOS/nixpkgs/nixos-unstable";
    nixpkgs-stable.url = "github:NixOS/nixpkgs/nixos-25.11";
  };

  outputs =
    { self
    , nixpkgs
    , nixpkgs-stable
    }:
    let
      inherit (nixpkgs) lib;
      forAllSystems = lib.genAttrs lib.systems.flakeExposed;

      importPkgs = path: attrs: import path (attrs // {
        config.allowAliases = false;
        overlays = [ self.overlays.default ];
      });

      packages' = forAllSystems (system: (
        let
          pkgs = importPkgs nixpkgs { inherit system; };
          inherit (pkgs) lib;

          overlayAttributes = lib.pipe (import ./. pkgs pkgs) [
            builtins.attrNames
            (lib.partition (n: lib.isDerivation pkgs.${n}))
          ];
          attributesToAttrset = attributes: lib.pipe attributes [
            (map (n: lib.nameValuePair n pkgs.${n}))
            lib.listToAttrs
          ];

        in
        {
          lib = attributesToAttrset overlayAttributes.wrong;
          packages = attributesToAttrset overlayAttributes.right;
        }
      ));

    in
    {
      # self: super: must be named final: prev: for `nix flake check` to be happy
      overlays = {
        default = final: prev: import ./overlays final prev;
        emacs = final: prev: import ./overlays/emacs.nix final prev;
        package = final: prev: import ./overlays/package.nix final prev;
      };
      # for backward compatibility, is safe to delete, not referenced anywhere
      overlay = self.overlays.default;

      hydraJobs =
        lib.genAttrs [ "x86_64-linux" "aarch64-linux" ] (system:
        let
          mkHydraJobs = pkgs:
            let
              inherit (pkgs) lib;

              filterNonDrvAttrs = s: lib.mapAttrs (_: v: if (lib.isDerivation v) then v else filterNonDrvAttrs v) (lib.filterAttrs (_: v: lib.isDerivation v || (builtins.typeOf v == "set" && ! builtins.hasAttr "__functor" v)) s);

              mkEmacsSet = emacs: filterNonDrvAttrs (pkgs.recurseIntoAttrs (pkgs.emacsPackagesFor emacs));

            in
            {
              emacsen = {
                inherit (pkgs) emacs-unstable emacs-unstable-nox;
                inherit (pkgs) emacs-unstable-pgtk;
                inherit (pkgs) emacs-git emacs-git-nox;
                inherit (pkgs) emacs-git-pgtk;
                inherit (pkgs) emacs-igc emacs-igc-pgtk;
              };
            };

        in
        {
          "stable" = mkHydraJobs (importPkgs nixpkgs-stable { inherit system; });
          "unstable" = mkHydraJobs (importPkgs nixpkgs { inherit system; });
        });

      packages = forAllSystems (system: packages'.${system}.packages);
      lib = forAllSystems (system: packages'.${system}.lib);
    };
}
```
## Agent's Thoughts (2026-04-01 16:44:07)


Now I understand how the emacs-overlay works. It's a proper flake that exports:
1. `overlays.default` - the main overlay
2. `overlays.emacs` - just the emacs version overlay
3. `overlays.package` - just the elpa/melpa packages overlay
4. `packages` - direct access to packages like `emacs-git`, `emacs-unstable-pgtk`, etc.

The proper way to use it in a flake is to:
1. Add it as an input
2. Apply the overlay to nixpkgs
3. Access the emacs packages from the overlay

Let me fix my implementation. The correct approach is to use the overlay properly:

```nix
nixpkgs.overlays = [ emacs-overlay.overlays.default ];
```

Or for flake-based usage, we can import nixpkgs with the overlay applied.

Looking at the flake.nix, the packages available are:
- `emacs-unstable-pgtk` - latest tagged release with PGTK
- `emacs-git-pgtk` - latest from master with PGTK
- `emacs-unstable` - latest tagged release
- `emacs-git` - latest from master

Let me redo the configuration properly using the overlay approach.

## Agent (2026-04-01 16:44:16)

Now I understand the emacs-overlay structure. It provides a proper flake with overlays and packages. Let me redo the configuration properly:



### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:44:27
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString: outputs =
    inputs@{
      self,
      nix-darwin,
      nixpkgs,
      nixpkgs-unstable,
      kmonad,
      emacs-overlay,
    }:
    let
      system = "aarch64-darwin";
      kmonad-exe = kmonad.packages."${system}".default;
      emacs-pkgs = import emacs-overlay.nix {
        system = "aarch64-darwin";
        nixpkgs = import nixpkgs { inherit system; overlays = [ emacs-overlay.overlay ]; };
      };
      configuration =
        { pkgs, ... }:
newString: outputs =
    inputs@{
      self,
      nix-darwin,
      nixpkgs,
      nixpkgs-unstable,
      kmonad,
      emacs-overlay,
    }:
    let
      system = "aarch64-darwin";
      kmonad-exe = kmonad.packages."${system}".default;
      configuration =
        { pkgs, ... }:

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -10,9 +10,5 @@
     let
       system = "aarch64-darwin";
       kmonad-exe = kmonad.packages."${system}".default;
-      emacs-pkgs = import emacs-overlay.nix {
-        system = "aarch64-darwin";
-        nixpkgs = import nixpkgs { inherit system; overlays = [ emacs-overlay.overlay ]; };
-      };
       configuration =
         { pkgs, ... }:
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:44:28)


Now I need to update the systemPackages to use the overlay properly. The correct way is to apply the overlay to nixpkgs and then access the emacs packages from there.


### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:44:33
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString: [
                 mpv
                 vim
                 cachix
                 git
                 emacs-pkgs.emacs29-pgtk
                 localsend
newString: [
                 mpv
                 vim
                 cachix
                 git
                 (import nixpkgs {
                   inherit system;
                   overlays = [ emacs-overlay.overlays.default ];
                 }).emacs-unstable-pgtk
                 localsend

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -3,5 +3,8 @@
                  vim
                  cachix
                  git
-                 emacs-pkgs.emacs29-pgtk
+                 (import nixpkgs {
+                   inherit system;
+                   overlays = [ emacs-overlay.overlays.default ];
+                 }).emacs-unstable-pgtk
                  localsend
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:44:34)


Now I need to update the emacs-lsp-booster reference as well.


### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:44:37
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString: emacs-pkgs.emacs-lsp-booster
newString: (import nixpkgs {
                   inherit system;
                   overlays = [ emacs-overlay.overlays.default ];
                 }).emacs-lsp-booster

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -1 +1,4 @@
-emacs-pkgs.emacs-lsp-booster
\ No newline at end of file
+(import nixpkgs {
+                   inherit system;
+                   overlays = [ emacs-overlay.overlays.default ];
+                 }).emacs-lsp-booster
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:44:38)


Let me verify the changes look correct by reading the relevant parts of the file.


### Tool Call [completed]: flake.nix

**Tool:** read
**Timestamp:** 2026-04-01 16:44:41
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
offset: 1
limit: 150

```
<path>/private/etc/nix-darwin/flake.nix</path>
<type>file</type>
<content>1: {
2:   description = "Fanshi1028's nix-darwin system flake";
3:   inputs = {
4:     nixpkgs.url = "github:NixOS/nixpkgs/nixpkgs-25.11-darwin";
5:     nixpkgs-unstable.url = "github:NixOS/nixpkgs/nixos-unstable";
6:     nix-darwin.url = "github:nix-darwin/nix-darwin/nix-darwin-25.11";
7:     nix-darwin.inputs.nixpkgs.follows = "nixpkgs";
8:     kmonad.url = "git+https://github.com/kmonad/kmonad?submodules=1&dir=nix";
9:     kmonad.inputs.nixpkgs.follows = "nixpkgs";
10:     emacs-overlay.url = "github:nix-community/emacs-overlay";
11:     emacs-overlay.inputs.nixpkgs.follows = "nixpkgs";
12:   };
13: 
14:   outputs =
15:     inputs@{
16:       self,
17:       nix-darwin,
18:       nixpkgs,
19:       nixpkgs-unstable,
20:       kmonad,
21:       emacs-overlay,
22:     }:
23:     let
24:       system = "aarch64-darwin";
25:       kmonad-exe = kmonad.packages."${system}".default;
26:       configuration =
27:         { pkgs, ... }:
28:         let
29:           kmonadConfigFile = pkgs.writeText "kmonad-config.kbd" (builtins.readFile ./home-row-mod.kbd);
30:         in
31:         {
32:           networking = {
33:             knownNetworkServices = [ "Wi-Fi" ];
34:             dns = [
35:               # adguard
36:               "94.140.14.14"
37:               "94.140.15.15"
38:               "2a10:50c0::ad1:ff"
39:               "2a10:50c0::ad2:ff"
40:               #quad9
41:               "9.9.9.9"
42:               "149.112.112.112"
43:               "2620:fe::fe"
44:               "2620:fe::9"
45:               #cloudflare
46:               "1.1.1.1"
47:               "1.0.0.1"
48:               "2606:4700:4700::1111"
49:               "2606:4700:4700::1001"
50:               # google
51:               "8.8.8.8"
52:               "8.8.4.4"
53:               "2001:4860:4860::8888"
54:               "2001:4860:4860::8844"
55:             ];
56:           };
57:           programs = {
58:             fish = {
59:               enable = true;
60:               promptInit = ''
61:                 ${builtins.readFile ./fish/fish_prompt.fish}
62:                 ${builtins.readFile ./fish/fish_right_prompt.fish}
63:               '';
64:               interactiveShellInit = "set -U fish_greeting";
65:             };
66:             direnv.enable = true;
67:           };
68: 
69:           fonts.packages = with pkgs; [
70:             nerd-fonts.symbols-only
71:             sarasa-gothic
72:           ];
73: 
74:           nix.channel.enable = false;
75: 
76:           nix.nixPath = [
77:             {
78:               nixpkgs = "flake:nixpkgs";
79:               nixpkgs-unstable = "flake:nixpkgs-unstable";
80:             }
81:           ];
82: 
83:           nix.extraOptions = ''
84:             keep-outputs = true
85:             keep-derivations = true
86:           '';
87: 
88:           nix.settings = {
89:             trusted-public-keys = [
90:               "fanshi1028-personal.cachix.org-1:XoynOisskxlhrHM+m5ytvodedJdAo8gKpam/L6/AmBI="
91:               "nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=  "
92:               "haskell-miso-cachix.cachix.org-1:m8hN1cvFMJtYib4tj+06xkKt5ABMSGfe8W7s40x1kQ0="
93:             ];
94:             trusted-substituters = [
95:               "https://fanshi1028-personal.cachix.org"
96:               "https://nix-community.cachix.org"
97:               "https://haskell-miso-cachix.cachix.org"
98:             ];
99:           };
100: 
101:           nix.registry = {
102:             nixpkgs = {
103:               flake = nixpkgs;
104:             };
105:             nixpkgs-unstable = {
106:               flake = nixpkgs-unstable;
107:             };
108:           };
109: 
110:           environment.variables = {
111:             EDITOR = "vim";
112:           };
113:           environment.systemPackages =
114:             (
115:               with pkgs;
116:               [
117:                 mpv
118:                 vim
119:                 cachix
120:                 git
121:                 (import nixpkgs {
122:                   inherit system;
123:                   overlays = [ emacs-overlay.overlays.default ];
124:                 }).emacs-unstable-pgtk
125:                 localsend
126:                 nix-output-monitor
127:                 ripgrep
128:                 zstd
129:                 nixfmt-rfc-style
130:                 ffmpeg
131:                 (import nixpkgs {
132:                   inherit system;
133:                   overlays = [ emacs-overlay.overlays.default ];
134:                 }).emacs-lsp-booster
135:                 python313Packages.huggingface-hub
136:                 # TEMP for doom emacs env: https://github.com/doomemacs/doomemacs/issues/6612#issuecomment-4150181313
137:                 fd
138:                 tree
139:               ]
140:               # default env to for using cabal inti to quick start haskell project
141:               ++ [
142:                 haskell.compiler.ghc9122
143:                 cabal-install
144:               ]
145:               ++ lib.optionals (system == "aarch64-darwin") [
146:                 (callPackage ./obs-studio.nix { })
147:               ]
148:             )
149:             ++ (with import nixpkgs-unstable { inherit system; }; [
150:               yt-dlp

(Showing lines 1-150 of 221. Use offset=151 to continue.)
</content>
```
## Agent's Thoughts (2026-04-01 16:44:48)


The configuration is now updated properly. The emacs-overlay is added as an input, and we're using the overlay to access `emacs-unstable-pgtk` and `emacs-lsp-booster` from the overlay.

However, I notice the code is a bit repetitive with the `import nixpkgs` calls. Let me refactor this to be cleaner by defining a local variable for the emacs-pkgs.

## Agent (2026-04-01 16:44:50)

Let me clean this up by defining a local variable for the emacs packages:



### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:44:57
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString: let
      system = "aarch64-darwin";
      kmonad-exe = kmonad.packages."${system}".default;
      configuration =
        { pkgs, ... }:
newString: let
      system = "aarch64-darwin";
      kmonad-exe = kmonad.packages."${system}".default;
      emacs-pkgs = import nixpkgs {
        inherit system;
        overlays = [ emacs-overlay.overlays.default ];
      };
      configuration =
        { pkgs, ... }:

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -1,5 +1,9 @@
 let
       system = "aarch64-darwin";
       kmonad-exe = kmonad.packages."${system}".default;
+      emacs-pkgs = import nixpkgs {
+        inherit system;
+        overlays = [ emacs-overlay.overlays.default ];
+      };
       configuration =
         { pkgs, ... }:
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:44:57)


Now I need to update the references to use the local variable instead of the inline import.


### Tool Call [completed]: flake.nix

**Tool:** edit
**Timestamp:** 2026-04-01 16:45:06
**Parameters:**
filePath: /private/etc/nix-darwin/flake.nix
oldString: [
                 mpv
                 vim
                 cachix
                 git
                 (import nixpkgs {
                   inherit system;
                   overlays = [ emacs-overlay.overlays.default ];
                 }).emacs-unstable-pgtk
                 localsend
                 nix-output-monitor
                 ripgrep
                 zstd
                 nixfmt-rfc-style
                 ffmpeg
                 (import nixpkgs {
                   inherit system;
                   overlays = [ emacs-overlay.overlays.default ];
                 }).emacs-lsp-booster
newString: [
                 mpv
                 vim
                 cachix
                 git
                 emacs-pkgs.emacs-unstable-pgtk
                 localsend
                 nix-output-monitor
                 ripgrep
                 zstd
                 nixfmt-rfc-style
                 ffmpeg
                 emacs-pkgs.emacs-lsp-booster

```
Edit applied successfully.





╭─────────╮
│ changes │
╰─────────╯

@@ -3,17 +3,11 @@
                  vim
                  cachix
                  git
-                 (import nixpkgs {
-                   inherit system;
-                   overlays = [ emacs-overlay.overlays.default ];
-                 }).emacs-unstable-pgtk
+                 emacs-pkgs.emacs-unstable-pgtk
                  localsend
                  nix-output-monitor
                  ripgrep
                  zstd
                  nixfmt-rfc-style
                  ffmpeg
-                 (import nixpkgs {
-                   inherit system;
-                   overlays = [ emacs-overlay.overlays.default ];
-                 }).emacs-lsp-booster
\ No newline at end of file
+                 emacs-pkgs.emacs-lsp-booster
\ No newline at end of file
```
## Agent's Thoughts (2026-04-01 16:45:07)


Done. Let me verify the final result looks correct.

## Agent (2026-04-01 16:45:07)

Done. The emacs-overlay has been properly integrated:

1. Added `emacs-overlay` as a flake input with `nixpkgs.follows` to use your nixpkgs version
2. Created `emacs-pkgs` variable that imports nixpkgs with the emacs-overlay applied
3. Replaced `emacs-pgtk` with `emacs-unstable-pgtk` (latest tagged Emacs with PGTK/Wayland support)
4. Added `emacs-lsp-booster` from the overlay

